<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CameraViewSample.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="CameraView Sample">
    <ContentPage.Content>
        <StackLayout>
            <Button
                x:Name="cameraButton"
                Clicked="CameraButton_Clicked"
                HorizontalOptions="Center"
                Text="Open Camera"
                VerticalOptions="Center" />
            <Button
                x:Name="segmentedControlButton"
                Clicked="SegmentedControlButton_Clicked"
                HorizontalOptions="Center"
                Text="Segmented Control"
                VerticalOptions="Center" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
